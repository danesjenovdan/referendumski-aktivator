apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: referendumski-aktivator-referendum-dostojna-smrt-2025-add-trailing-slash
spec:
  redirectRegex:
    regex: (/referendum/dostojna-smrt/referendumski-aktivator)(\?.*)?$
    replacement: ${1}/${2}
    permanent: false
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: referendumski-aktivator-referendum-dostojna-smrt-2025-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - /referendum/dostojna-smrt/referendumski-aktivator
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: referendumski-aktivator-referendum-dostojna-smrt-2025
  annotations:
    # Do not put cert manager here, root redirect ingress (in `ops` repo) will handle it.
    # If we put it here, they will both try to issue the certificate and letsencrypt will rate limit us.
    traefik.ingress.kubernetes.io/router.middlewares: |
      traefik-redirect-to-https@kubernetescrd,
      vodici-redirect-domain@kubernetescrd,
      vodici-referendumski-aktivator-referendum-dostojna-smrt-2025-add-trailing-slash@kubernetescrd,
      vodici-referendumski-aktivator-referendum-dostojna-smrt-2025-strip-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: vodici.danesjenovdan.si
      http:
        paths:
          - path: /referendum/dostojna-smrt/referendumski-aktivator
            pathType: Prefix
            backend:
              service:
                name: referendumski-aktivator-referendum-dostojna-smrt-2025
                port:
                  number: 80
